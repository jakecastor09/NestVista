import Hero from '../Hero/Hero';
import Listing from '../Listing/Listing';

const ListingPage = () => {
  window.scrollTo(0, 0);
  return (
    <>
      <Hero />
      <Listing />
    </>
  );
};

export default ListingPage;
